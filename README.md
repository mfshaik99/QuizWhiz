# QuizWhiz
A  a multi-user online quiz platform that allows 1–100 concurrent users to join a quiz using a shared link, attempt the same set of questions with shuffled options, and view a leaderboard based on accuracy and response time after quiz completion.
